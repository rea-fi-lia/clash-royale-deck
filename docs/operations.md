# 運用メモ — 壊れ方と、気づく仕組み

> **著者**: jo ＋ Claude
> **最終更新**: 2026-08-11
> **関連**: [data-collection.md](./data-collection.md)（収集の設計）／[monetization.md](./monetization.md)（課金の設計）

## 2026-08-11 の障害：収集が3時間26分止まった

```
08/11 18:34 JST  ✅ 最後の正常
08/11 18:44 JST  ❌ … 11回連続で失敗 …
08/11 21:48 JST  ❌
08/11 22:01 JST  ✅ 復旧
```

**原因**：帯別イベントの保持上限を 800→2400 へ引き上げた副作用。
`(旧22万件).concat(今回分)` で巨大な中間配列を作ってから `filter → sort` していたため、
保持107,372件／候補219,202件の絞り込み中に **JavaScript heap out of memory（exit 134）**。

**直した3点**
1. `concat` をやめ、1本の配列へ push しながら**その場で期限切れを捨てる**（中間配列を作らない）。旧配列の参照も明示的に手放す
2. `NODE_OPTIONS=--max-old-space-size=8192`（GitHubランナーは16GB）
3. 上限を現実的な値へ：**帯 2400→1600 ／ 全体 120000→80000**（47帯×1600＝75,200。頭打ちだった800の倍で、メモリにも収まる）

**実害**
- 全体統計：**軽微**。毎回ちがうプレイヤーを標本として拾う方式なので、止まった分は「その3時間に拾えたはずの標本が減った」だけ。既存データは壊れていない。3日窓なので数時間で厚みは戻る
- 個人データ（先駆けタグ）：**ゼロ**。joの最終試合は8/3で、停止時間帯にプレイしていなかった。ただし3時間26分は**75分ルールを超えている**ので、プレイしていたら取りこぼしていた

## ★本当の問題：落ちても誰も気づかなかった

11回失敗しても検知されず、joが「最終更新が17:49で止まってる」と気づいて発覚した。
**気づける仕組みが無いことが最大の欠陥**なので、通知を入れた。

### 通知の仕組み

| ファイル | 役割 |
|---|---|
| `.github/actions/notify/action.yml` | ★**単一ソース**。鳴らし方を変えたいときはこの1枚を直せば全ワークフローに効く |
| `tools/notify.js` | 送信そのもの。Discord / Slack / LINE。**Secretが入っているものだけ動く** |
| `tools/digest.js` | 1日1回の数字サマリー |

- **連続失敗のしきい値をワークフローごとに変えている**：毎時のcollectは2回連続（1回の瞬断では鳴らさない）、1日1回のものは1回で鳴らす（次は24時間後だから）
- **復旧したときも1回**知らせる
- 失敗時は**落ちたステップのログ末尾1,200字を本文に添える**。Discordは等幅なので原因がそのまま読める

### 通知が付いているワークフロー

| ワークフロー | 何が止まると困るか | しきい値 |
|---|---|---|
| `collect.yml`（毎時2回） | サイトの数字が更新されない | 2回連続 |
| `build-card-stats.yml`（毎日13:40 JST） | 圏内判定などの導出タグが古いままになる | 1回 |
| `check-card-images.yml`（毎日14:10 JST） | 画像が表示できない状態を見逃す | 1回 |
| `export-tags.yml`（毎日13:50 JST） | シートへの同期が止まる | 1回 |
| `digest.yml`（毎日07:00 JST） | サマリー自体が作れない＝APIが落ちている疑い | 1回 |

### 日次サマリー（`digest.yml`・毎朝7時）
静かなときに何も返ってこないと、生きているのか分からない。1日1回、**前日比つきの数字**を返す：

```
📊 CRDB 日次サマリー
収集した試合    123,440戦 (+3,440)
見たプレイヤー  68,017人 (+1,017)
毎時の新規      956人/回 (+56)
トロフィー帯    47/47 帯
帯データ        74,830件 (+830)
カード          122枚
最終更新        08-11 22:11 JST（29分前）
```

帯が47未満・カードが122未満・最終更新が90分超のときは⚠️を付けて赤で送る。
前日の値はリポジトリを汚さないよう **Actionsのキャッシュ**で持ち越す。

| 送り先 | 必要なSecret | 手間 | 費用 |
|---|---|---|---|
| **Discord** ★推奨 | `DISCORD_WEBHOOK_URL` | Webhook URLを1本作るだけ（約3分） | ¥0・通数無制限 |
| Slack | `SLACK_WEBHOOK_URL` | Webhook作成（約5分） | ¥0（無料枠は履歴90日） |
| LINE | `LINE_PUSH_TOKEN`／`LINE_PUSH_TO` | 公式アカウント作成→Messaging APIチャネル→トークン発行→自分のuserId取得（約30分） | 無料枠 月200通 |

### なぜ開発通知はDiscordを推すか（2026-08-11に検討）
1. **立ち上げ摩擦がほぼゼロ**：サーバー内で「連携サービス→ウェブフック→URLをコピー」。審査もアプリ作成もトークン発行もない
2. **ログが読める**：等幅フォントのコードブロックが使える。LINEは等幅が無く、スタックトレースが折り返して読めない
3. **色で状態が分かる**：embedで赤＝障害／緑＝復旧。一目で判別できる
4. **通数を気にしなくていい**：LINE公式アカウントの無料枠は月200通（配信数×人数）。開発通知でこれを食うのはもったいない
5. **流れが残って検索できる**：「前も同じ落ち方したっけ」を後から追える

→ `tools/notify.js` は3つ全部に対応。**Secretを入れたものだけが動く**ので、後から足しても引いてもコード変更は要らない。

### ⚠️ 園の公式LINEは使わない
`line-harness` に登録されているのは**梅乃園幼稚園の公式アカウント1つだけ**（channelId 1657652010・フォロワー58）。
これは保護者向けなので、**開発通知を混ぜてはいけない**。
LINEで受け取るなら **ReA名義のLINE公式アカウントを別に作る**（joの作業）。

**joがやること（Discordの場合・約3分）**
1. Discordで自分用のサーバーを作る（無ければ。「＋」→「オリジナルの作成」→「自分と友達のため」）
2. 通知用チャンネルを作る（例 `#crdb-alerts`）
3. チャンネル名の右の⚙️→**連携サービス→ウェブフック→新しいウェブフック→ウェブフックURLをコピー**
4. GitHubリポジトリの Settings → Secrets and variables → Actions → New repository secret
   名前 `DISCORD_WEBHOOK_URL` / 値＝コピーしたURL

**確かめ方**（どちらでもよい）
- GitHubの Actions タブ → `daily digest` → Run workflow → 数字のサマリーが届けば成功
- 手元で確かめるなら：

```bash
DISCORD_WEBHOOK_URL='貼る' node tools/notify.js --test
```

赤（失敗の見え方）と緑（復旧の見え方）が1通ずつ届く。

**LINEで受けたい場合**
1. LINE Official Account Manager で **ReA名義の公式アカウント**を作る（無料）
2. LINE Developers で Messaging API チャネルを作り、**チャネルアクセストークン（長期）** を発行
3. 自分のLINEでそのアカウントを友だち追加し、**自分のuserId** を取得
4. Secrets に `LINE_PUSH_TOKEN` と `LINE_PUSH_TO` を登録

## ReAの公式LINE／Discordをどう位置づけるか（2026-08-11）

**用途が2つある。混ぜない。**

| | 開発通知（自分だけが見る） | ファン向け（外に出す） |
|---|---|---|
| 相手 | jo 1人 | ReAのリスナー |
| 目的 | 落ちたら気づく | アルバム（11月末）・宇宙コンサート（2027年1月）を届ける |
| 最適 | **Discord**（上の5点） | **まだ決めない**。11月に向けて別途 |

ファン向けは性質が違う。**LINEは「届く」（日本の一般層への到達力が圧倒的・ただし通数課金）、Discordは「住める」（コミュニティとして機能する・無料・ただし来てくれる人は濃い層に偏る）。**
ReAの目的が「入って住める世界をエクスポートする」ことなら、構造的にはDiscordの形（住人同士が話す場）の方が近い。
ただしこれは**joが決めること**で、通知の都合で決めてはいけない。だから今は通知だけDiscordで通し、ファン向けは切り離しておく。

### line-harnessに2つ目のアカウントを足す件（調査結果）
- OSS本体は**マルチアカウント対応済み**。`POST /api/line-accounts` で `channelId / name / channelAccessToken / channelSecret` を登録するだけ
- Webhookは**単一パス `/webhook`** のまま。**署名でどのアカウント宛かを判別**する（まず環境変数のsecretで試し、外れたら `line_accounts` を順に照合）。つまり2つ目のチャネルのWebhook URLも同じURLでよい
- 友だち・タグ・配信は**アカウントごとに分離**され、`users` テーブルのUUIDで「同じ人が両方を友だち追加している」を横断把握できる
- ⚠️ **ただし今のharnessは園（umenosonoai）のCloudflareアカウント・園のD1の上にある。**
  ReAをそこに同居させると、園のAPIキー1本でReAの友だちデータも読めるし、逆も同じ。
  園の都合（アカウント停止・支払い・退職者への鍵の扱い）がReAを巻き込む。
  → **ReA用は jo 自身のCloudflareアカウント（cfjo）に別インスタンスを建てる**のが正しい。`npx create-line-harness` でもう1台。¥0。
  MCPは2つ登録すればよく、混同も起きない。

## 上限を触るときの心得

収集の枠（シード数・保持件数）は**メモリと直結**している。上げるときは：
1. まず1段だけ上げて、**次のrunのログで `保持 X/Y` と実行時間を見る**
2. `Y`（候補件数）が20万を超えるなら、絞り込みの実装がメモリを食っていないか疑う
3. `rate-limit 429hits=` は別問題（APIの制限）。**429が0でもメモリで落ちる**ことがある ← 今回これ

## 見張るところ

| 何を | どこで | 正常値 |
|---|---|---|
| 収集が生きているか | `/api/meta` の `decks.updated` | 1時間以内 |
| API制限 | collectログの `rate-limit 429hits=` | 0 |
| メモリ | collectログに `heap` が出ていないこと | 出ない |
| 帯のカバー | collectログの `帯数` | 47 |
| 発見の飽和 | collectログの `clan-crawl newPlayers=` | 0になるまで拡大の余地あり |
| 先駆けタグ | collectログの `pilot ...` | `対象N件（先駆け1 / 課金M）` |
